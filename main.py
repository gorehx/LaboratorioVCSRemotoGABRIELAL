import math
b = int(input("inserte el numero para calcular el cuadrado de este"))
d = float(input("inserte el numero para calcular la raiz cuadrada de este"))
cuadrado= (b*b)
raiz= math.sqrt(d)
print ("el cuadrado del primer numero es:", cuadrado, ",y la raiz cuadrada del segundo numero es:", raiz)
